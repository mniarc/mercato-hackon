# T96 - Integrate and prove the shared agent harness

State: ready
Priority: P0-immediate
Depends on: T95 source handoff; T93 runtime selection; T94 journal/index.
Owns: coordinator integration, runtime validation and remaining cross-owner wiring.
Sources: user request for demonstrable end-to-end teammate agent integration.

## Deliver

- Review the detached T95 handoff once and reconcile it with current T79, T93 and
  T94 changes without losing teammate behavior or pending main changes.
- Run focused checks for the changed mode/observation seams, then the connected
  fixture demo on the persistent runtime. No database rebuild, duplicate journey
  assertions, live model calls, real payment or publication.
- Generate the integration report from that run's actual journal; show reached
  agent/handoff IDs, normal waits, failures and unproved integrations separately.
- Use the teammate inventory to assign genuinely missing app connections or
  distinct journey coverage. A passing single path is not proof of every agent.
- Commit a coherent verified harness milestone and retain honest live-proof gaps.

## Done when

Fixture execution uses real teammate wrappers and native app transitions through
the shared scenario; mode isolation is verified; report evidence comes from actual
native results and consumed handoffs. Live execution remains opt-in and unproved
until separately authorized. Preserve any uncovered integration gaps as concrete
tasks rather than treating this harness milestone as full product completion.
