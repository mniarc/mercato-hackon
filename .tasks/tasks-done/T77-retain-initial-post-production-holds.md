# T77 - Keep initial post-production holds visible and pending

State: done
Verification evidence: `9d14fa5b7`; four focused suites (75 checks) passed across
post handoff, native graph, scoped projection and staff case rendering. Missing
or blocked output remains visible and waiting; only a genuine invitation ends
review. This is not a producer-resume or full native exception-journey claim.
Sources: F31-1 AC3-4; F32-1 AC1.
Owns: operations postExecution review handoff/contracts, existing case projection/UI;
separate graph owner handles native G transitions and their focused checks.

## Deliver

- Preserve exact saved production reasons and evidence when configuration,
  dependencies, interrupted execution or QA prevent a post-review invitation.
- Native G ends this handoff only after a real invitation; otherwise retain a
  visible wait with configuration/dependency/execution/QA inspection guidance.
- Keep existing genuine employee escalation routing and exact QA-ready invitation.
  Missing or malformed production output is an explicit reconciliation hold,
  not successful completion or approval.

## Done when

- Focused existing handoff, graph and case-view checks cover the saved hold and
  real invitation; scope mismatches cannot become a case's outcome.
- Staff can inspect the original result and linked native process. No new retry,
  spending authority, employee approval or automatic resume is implied.

## Limits

This closes the lost-handoff state, not full targeted evidence recovery (F31-2)
or all resumable execution paths. No new worker, default budget or runtime reset.
