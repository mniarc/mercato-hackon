# T49 - Run the teammate post author and editor from the chosen topic

State: done
Delivered: `6d78b0ba3`, connected proof in `23bc9e681` with native commit-barrier fix `672345013`. The headed journey passed native post author/editor execution, actual QA exhaustion and saved employee escalation with local intelligence. Accepted upstream foundations were seeded; no paid/live-model proof is claimed.
Depends on: T28 connected plan decision and saved instruction
Sources: F30-1, F30-2, F31-1
Owns: `agency_research/lib/postExecution/**`; bounded pin support in existing `research/steps/{context,post,postQa}.ts`; native `agency_operations/lib/postExecution/**`. Root owns public service, workflow, execution policy and employee wiring.

## Deliver

- Continue 7.1 through existing teammate author 7.2 and editor 7.3 from the exact
  current instruction and its stored client topic decision. Reuse their workers,
  prompts, compiler outputs, repair limits and exception contract.
- Pin the instruction, accepted ToV and order; keep repairs within this execution.
  The writer's own draft is not an unapproved foundation or simulated client consent.
- Require a separate configured post-execution spend cap; keep live activation off.
  Replay saved execution instead of regenerating on duplicate handoffs. Interrupted,
  budget-paused and exhausted-QA work retains real references and a named state.
- Show the saved post/editor outcome in the existing employee process section.
  Stop at QA/readiness. No inferred client approval, publication or paid demo calls.

## Done when

The native instruction handoff can call the real phase-only producer and expose
the exact post and QA result. Check only the new pin/replay boundary and combined
types; existing author/editor tests remain authoritative. This bounded slice does
not complete F31-2 targeted research returns or T29's client review/publication chain.
