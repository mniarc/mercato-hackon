# T80 - Representative analysis QA escalation to employee

State: queued (after the primary connected production demo)
Sources: F08-2 AC3,5; user-requested representative failure demonstration.
Owns: separate TC-AGENCY-004-analysis-qa-exception.spec.ts and bounded
support/analysisQaException intelligence; coordinator owns launcher selection/runtime.

## Deliver

- Reuse native purchase/production setup, recorded research sources and local
  intelligence provider. Real zero-charge purchase starts real teammate research.
- Substitute only QA intelligence with a clearly labelled representative blocking
  staff-owned finding, `fix_step: null`. Native 3.7 computes `exception`; real E.1
  persists `invalid_state` and routes an employee task. Never insert successful
  outputs, exception documents or tasks directly, or change QA logic.
- Headed staff view shows the same saved finding, exception and case ledger.
  Record exact case/workflow/task/version references and finding in existing test
  output before existing scoped cleanup; no new harness or Markdown inventory.

## Done when

One separately selected no-paid journey proves persisted 3.7 exception -> E.1
`invalid_state` -> pending native employee task and visible evidence. Preserve
the distinction from `qa_exhausted`; do not approve, resolve or resume the hold.

## Limits

This represents the teammate screenshot's failure class, not its unknown exact
finding, cost or repair history. No claim to reproduce their inaccessible data.
Run only after an explicit shared-runtime lease; no database reset or paid calls.
