# T33 - Show the real agency process and an honest end-to-end demo

State: active
Depends on: existing case/query UI; add each domain projection after its owning task
Owns: `agency_operations/lib/processProjection/**`, self-contained employee process UI,
and the existing canonical demo scenario
Sources: F52-1, F53-1, F54-1, F55-1, F56-1, F57-1, F58-1, F59-1

## Deliver

- Extend existing employee case/query contracts from persisted native workflow,
  agent, artifact and UserTask records: role, inputs/results, dependency links,
  client wait, dependency block or owned employee exception. No shadow lifecycle.
- Preserve exact lineage and separate customer approval from employee authority;
  keep the deterministic baseline independent of Enterprise and label agency
  functionality as agency-owned. The selected agent execution requires Enterprise;
  do not claim equivalent OSS execution or complete F54 coverage from scaffolds.
- Extend the single canonical demo only at newly delivered seams. Distinguish
  earlier execution/test payment from live generation, genuine customer branch
  choice and current consents from fixtures, provider proof from unknown outcome.
- Keep a truthful fallback with the current blocked state; timing never bypasses
  consent or creates success. Both intended demo directions need a real rehearsal.

## Done when

- The next delivered domain appears from real records with a focused scoped-query
  check. A full demo is claimed only after its upstream tasks genuinely work;
  measure that path once on the persistent environment, not a new parallel harness.

## Constraints

- Reuse teammate customer UI, native employee surfaces and existing observability.
  No invented SLA, fake earlier paid history, or speculative all-domain dashboard.
