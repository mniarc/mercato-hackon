# T98 — Bind strategy and downstream work to the sole ToV specialist

State: active (P0; initial integration and correction source checked; connected native proof pending)
Immediate scope: connect the existing revision producer to saved client/QA requests,
native continuation and fresh paired review; preserve initial integration.
Sources: F22-1, F23-1, F24-1, F24-2; explicit user architecture decision.
Depends on: T97 case-bound specialist intake.

Replace reachable `agency_research.tov_writer` production/repair with exact,
immutable `agency_tov` KLI-TOV references. Preserve strategy authorship, assess
the real pair, and record client acceptance without copying specialist content
into research-owned documents. Missing corpus/result is an actionable native
wait; the saved specialist outcome must continue the same case toward paired
review. ToV corrections return to its specialist, never a research rewrite.

The initial intake/strategy/downstream integration passed 22 focused suites /
174 tests and app typecheck. Additive specialist-owned `revise` now reuses the
same synthesizer and saved evidence, requires exact native-definition execution
policy, preserves unaffected fields and writes an immutable version. Its two
focused checks and app typecheck passed. It does not claim an enforced monetary
cap or completed correction journey.

Correction caller implemented (F22-1 AC4, F23-1 AC4, F24-1 AC2–3, F25-1 AC2):
saved client task/original/native G decision or exact ToV-only blocking pair QA
binds the specialist revision. Native G then reassesses the unchanged strategy
against the new exact ToV and uses the existing review/employee handoffs.
Mixed or unsupported QA faults retain employee routing. No second writer,
inherited ToV acceptance, automatic repeat repair, or borrowed initial budget.
Five focused suites / 43 tests and the app typecheck passed after integration;
the correction native journey remains pending.

Execution remains opt-in: `configure-triage --tov-revision-policy-file` accepts
`enabled:true`, `maxAgentCalls:1`, `runTimeoutMs` and `pairQaMaxCostPln`.
The policy is pinned to the native definition/version; existing definitions
require native version publishing, never overwrite. ToV and analysis execution
flags are also required. The specialist limit is one synthesis call plus timeout,
not an enforced monetary ceiling; native provider retries remain unchanged.
Remaining: genuine native initial specialist-to-pair and correction-to-fresh-review
proof, including configured fixture intelligence rather than saved result seeding.

Owners: strategy/QA/pair projection (employee_process_delivery), scoped specialist
reader (integration_evidence_index), acceptance/downstream reads
(expand_feature_captures), case-bound intake/continuation (coverage_html + root).
Keep historical versions readable. No paid calls, invented voice fields or QA passes.
Retire legacy whole-pipeline `--through 5.4` and later before any work; retain
research-only through 4.2 and historical reads. Canonical native phases replace it.

Done: connected source + focused checks, then genuine native specialist-to-pair
proof; old writer fixtures/history do not prove this new authoritative path.
