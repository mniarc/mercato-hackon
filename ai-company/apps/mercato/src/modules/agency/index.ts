import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'agency',
  title: 'AI Agency',
  version: '0.1.0',
  description: 'AI marketing agency — START KOMUNIKACJI order fulfilment (customer portal + case).',
  author: 'Hackathon',
  license: 'MIT',
}

export default metadata
